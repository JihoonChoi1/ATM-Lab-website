# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# runtime uploads (7-8) — admin-uploaded images, never committed
/public/uploads

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# raw pg_dump source for prisma/seed-data.sql — contains User hashes + is
# PG17-only custom format; the committed seed-data.sql is the sanitized artifact
dump.dump

# project planning / local-only docs
Steps.md
html_files

/app/generated/prisma

/docs
